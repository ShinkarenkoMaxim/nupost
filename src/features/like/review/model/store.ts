import { createEffect, createEvent } from 'effector';
import * as api from './api';

// export const likeClicked = createEvent();
// export const likeReviewFx = createEffect<{ reviewId: number }>(api.likeReview);
